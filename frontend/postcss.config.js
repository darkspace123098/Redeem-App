// Intentionally left minimal to avoid loading Tailwind as a PostCSS plugin.
// We are using the Tailwind CDN at runtime instead of PostCSS processing in this dev container.
module.exports = {
  plugins: {},
};
